# Double-the-negative-practice
write a java method that iterates through an array of integers.if any element is negative,you must double its value. the method should modify the array in place and not returna value
